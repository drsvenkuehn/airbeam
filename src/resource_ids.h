#pragma once

// src/resource_ids.h
// Proxy header so that source files under src/ can reference string resource
// IDs without using back-tracking relative paths.  The canonical #define list
// lives next to the resource script in resources/resource_ids.h.
#include "../resources/resource_ids.h"
