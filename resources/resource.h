#pragma once

// Convenience wrapper — canonical IDS_* definitions are in resource_ids.h.
// Include this header (or resource_ids.h directly) from C++ translation units
// that need string resource IDs.
#include "resource_ids.h"
